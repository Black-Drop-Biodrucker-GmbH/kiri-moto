/** stub — CSG/manifold is only used for CAM visualization, not FDM slicing */

export const CSG = {
    union:     () => null,
    subtract:  () => null,
    intersect: () => null,
    moduleOp:  () => null,
};
